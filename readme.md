![telegram-cloud-photo-size-2-5267072911288241912-y](https://github.com/user-attachments/assets/9221b9fc-c649-4f47-bd1b-6a3752dd9fb8)

## 🌟 Подготовка окружения к мастер-классу

Выполните все шаги заранее.

---

# 1. 📥 Установить необходимые программы

## **🟨 IDE (редактор кода)**

Установите любую современную IDE:

* **VS Code** — [https://code.visualstudio.com](https://code.visualstudio.com)
* **WebStorm** — [https://www.jetbrains.com/webstorm](https://www.jetbrains.com/webstorm)
* **Или любая другая IDE, которая поддерживает JavaScript**

После установки убедитесь, что IDE открывается и корректно работает с файлами.

---

## **🟦 Node.js (LTS)**

Скачать:
[https://nodejs.org](https://nodejs.org)

После установки выполнить:

```bash
node -v
npm -v
```

**Что вы должны увидеть:**

* Появится номер версии.
* ❌ Ошибок быть не должно.

Пример (актуальная LTS):

```
node -v
v20.18.0

npm -v
10.8.2
```

---

## **🟪 Git**

Скачать:
[https://git-scm.com](https://git-scm.com)

Проверить установку:

```bash
git --version
```

Пример:

```
git version 2.45.1
```

---

## **🐳 Docker Desktop**

Скачать:
[https://www.docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop)

### 🟩 Как убедиться, что Docker запущен

1. Увидеть иконку Docker в системном трее / панели.
2. Выполнить:

   ```bash
   docker --version
   ```

   Пример корректного вывода:

   ```
   Docker version 27.1.2, build cb74dfc
   ```

   ❌ Ошибок быть не должно.

---

# 2. 🛠 Подготовить локальную базу данных

Создайте папку, например:

```
squid-masterclass
```

Внутри создайте файл **docker-compose.yml**:

```yaml
services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: squid_admin
      POSTGRES_PASSWORD: squid_pass
      POSTGRES_DB: squid_game
    ports:
      - "5432:5432"

  adminer:
    image: adminer
    ports:
      - "8080:8080"
    environment:
      ADMINER_DEFAULT_SERVER: postgres
```

Запустить контейнеры:

```bash
docker compose up -d
```

Если Docker работает — база данных и веб-интерфейс запустятся автоматически. 🚀

---

# 3. 🧭 Как открыть Adminer

**Adminer (веб-интерфейс для БД)**
Открыть в браузере:
👉 [http://localhost:8080](http://localhost:8080)

Использовать следующие данные:

* **System:** PostgreSQL
* **Server:** postgres
* **User:** squid_admin
* **Password:** squid_pass
* **Database:** squid_game

---

Добавьте блок **IDE** сразу после Docker — структурно так же, как остальные пункты:

---

# 4. 🎉 Готово!

Если программы установлены, Docker запущен, а `docker compose up -d` не даёт ошибок — вы полностью готовы к мастер-классу.
